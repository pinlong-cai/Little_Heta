# heta recall

Retrieve saved memory relevant to a query.

```bash
heta recall "what did we decide about the database?"
```

Use this when you want personal memory only, without searching the document
wiki.

Options:

```bash
heta recall "..." --top-k 10 --debug
```

`--debug` shows ranking and evidence details.

